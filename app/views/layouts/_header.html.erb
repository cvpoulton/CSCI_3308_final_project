<%= if flash[:error] then "#{flash[:error]}" end %>
<%= if flash[:message] then "#{flash[:message]}" end %>
<%= if @current_user != nil then "#{@current_user.first_name} #{@current_user.last_name} is logged in" else "No user signed in" end %>
