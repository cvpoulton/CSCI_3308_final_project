=provide(:title, 'Newsfeed')

%h1 Newsfeed Page

= form_tag post_path({:post_user=>@current_user.id, :from_newsfeed => 1}) do
  = text_field :post, :message, :class => "span5"
  = submit_tag "Post status", :class => "btn btn-info"

= render 'viewing/postlist'
