%h1 Profile Preferences

= link_to 'Newsfeed', newsfeed_path
= link_to 'Profile Page', preferences_path(:userProfile => session[:login_id])
= link_to 'Search/Add Users', friends_path

= form_tag update_path do

  = label :current_user, :interests, 'Interests'
  = text_field :current_user, :interests
  %br
  = label :current_user, :quotes, 'Quotes'
  = text_field :current_user, :quotes
  %br
  = submit_tag 'Update Profile'

%h2 Pending Friends

- if @pending_friends.length() != 0
  %table#pending_friends
    %tbody
      - @pending_friends.each do |pending_friend|
        %tr
          %td= link_to "#{pending_friend.first_name} #{pending_friend.last_name}"
          %td= "Add"
          %td= "Deny"
- if @pending_friends.length() == 0
  No pending friends!


%h2 Current Friends

- if @friends.length() != 0
  %table#friends
    %tbody
      - @friends.each do |friend|
        %tr
          %td= link_to "#{friend.first_name} #{friend.last_name}"
          %td= "Unfriend"
- if @friends.length() == 0
  No friends!
